import {windowWidth, windowHeight} from '../utils/Dimentions';
export const HEIGHT_SEARCH = 56;
export const SIZE = 68;
export const ICON_SIZE = SIZE * 0.5;
export const SPACING = 10;
export const CONTENT_OFFSET_THRESHOLD = 200 ;
export const SHOW_GO_TOP = 58 ;
export const HEIGHT_SCROLLVIEW = windowWidth/3 ; 

const s = windowWidth * 0.68;

export const tutorial2Species = {
  ITEM_WIDTH: s,
  ITEM_HEIGHT: s * 0.5,
  RADIUS: 18,
  SPACING,
  FULL_SIZE: s + SPACING * 2,
};
export default tutorial2Species;
